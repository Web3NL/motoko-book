{
  "type": "module",
  "devDependencies": {
    "@types/node": "^18.11.18",
    "motoko": "^3.3.0",
    "tsc": "^2.0.4",
    "vite": "^4.2.0"
  },
  "scripts": {
    "dev": "cd comments/frontend/ && npm run dev",
    "serve": "mdbook serve",
    "generate": "dfx generate commentsbackend",
    "domain": "cp -r domain/.well-known/ book/ && cp domain/.ic-assets.json book/",
    "build-book": "npx vite build && mdbook build && npm run domain",
    "build-comments": "dfx build commentsbackend && npm run generate && cd comments/frontend/ && npm run build",
    "build": "npm run build-book && npm run build-comments"
  }
}
