{
  "devDependencies": {
    "@types/node": "^18.11.18",
    "motoko": "^3.3.0"
  },
  "scripts": {
    "build": "mdbook build && cp -r domain/.well-known/ book/ && cp domain/.ic-assets.json book/"
  }
}
